import { DisplayIntegerPipe } from './display-integer';

describe('DisplayIntegerPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayIntegerPipe();
    expect(pipe).toBeTruthy();
  });
});
