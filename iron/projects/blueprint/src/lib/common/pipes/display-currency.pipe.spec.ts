import { DisplayCurrencyPipe } from './display-currency.pipe';

describe('DisplayCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
